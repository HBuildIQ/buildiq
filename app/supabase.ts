import { createClient } from "@supabase/supabase-js";

const supabaseUrl = "https://sysdgjhtwuoatinjfieo.supabase.co";

const supabaseKey =
  "anon public

export const supabase = createClient(
  supabaseUrl,
  supabaseKey
);